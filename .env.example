# Used by docker compose & when connecting to db
POSTGRES_PASSWORD="postgres"
POSTGRES_USER="postgres"
POSTGRES_DB="npm-rest"
POSTGRES_PORT="5432"

# Used when connecting to db
POSTGRES_HOST="127.0.0.1"

# Used some places for logging
SENTRY_DSN=""
